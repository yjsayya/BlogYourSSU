spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/blog
    username: root
    password: wndehd
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true